# Imported Documentation

These files were copied into Git during Phase 3 of the CAL restructure.

The original loose files still exist outside `cal-app` because `/Users/donnaile/dev/CAL` is not the single Git root yet. Do not edit the loose originals for new work. Edit the tracked copies in this folder or move the content into a canonical doc during a later docs cleanup phase.
